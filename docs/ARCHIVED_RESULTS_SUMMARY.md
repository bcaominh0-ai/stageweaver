# Archived Results Summary

Historical run directories are not kept in the active repository.

This cleanup package did not contain active `result/stageweaver/r001_*` through `r034_*` directories, partial aborted runs, invalid runs, smoke-only result folders, or old judged-summary files. If those artifacts exist in a fuller checkout, move them outside the active repo or store a small fixture under `tests/fixtures/` only when needed for lightweight tests.
